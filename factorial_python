number = int(input('Choose a number to calculate your factorial: '))
factorial = 1
for item in range(1, number + 1):
  factorial = factorial * item
print('Factorial of {number} is {factorial}.'.format(number=number, factorial=factorial))
